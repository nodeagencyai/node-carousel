<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 {{WIDTH}} {{HEIGHT}}" width="{{WIDTH}}" height="{{HEIGHT}}">
  <defs>
    <style>
      @import url('https://fonts.googleapis.com/css2?family={{FONT_DISPLAY_URL}}:wght@700;800&amp;family={{FONT_BODY_URL}}:wght@400;500;600&amp;display=swap');
      .hook { font-family: '{{FONT_DISPLAY}}', serif; font-weight: 800; font-size: 72px; fill: {{COLOR_TEXT}}; }
      .button-text-primary { font-family: '{{FONT_BODY}}', sans-serif; font-weight: 600; font-size: 38px; fill: {{BG_COLOR}}; letter-spacing: 0.02em; }
      .button-text-secondary { font-family: '{{FONT_BODY}}', sans-serif; font-weight: 600; font-size: 38px; fill: {{COLOR_ACCENT}}; letter-spacing: 0.02em; }
      .subtext { font-family: '{{FONT_BODY}}', sans-serif; font-weight: 400; font-size: 28px; fill: {{COLOR_MUTED}}; }
      .handle { font-family: '{{FONT_BODY}}', sans-serif; font-weight: 500; font-size: 26px; fill: {{COLOR_TEXT}}; letter-spacing: 0.05em; }
    </style>
  </defs>
  {{BACKGROUND}}
  {{DECORATIONS}}
  <text x="{{CENTER_X}}" y="{{CTA_HOOK_Y}}" class="hook" text-anchor="middle">
    <tspan x="{{CENTER_X}}" dy="0">{{CTA_HOOK_LINE_1}}</tspan>
    <tspan x="{{CENTER_X}}" dy="86">{{CTA_HOOK_LINE_2}}</tspan>
  </text>
  <rect x="120" y="780" width="400" height="120" rx="60" fill="{{COLOR_ACCENT}}"/>
  <text x="320" y="858" class="button-text-primary" text-anchor="middle">{{CTA_BUTTON}}</text>
  <rect x="560" y="780" width="400" height="120" rx="60" fill="none" stroke="{{COLOR_ACCENT}}" stroke-width="3"/>
  <text x="760" y="858" class="button-text-secondary" text-anchor="middle">{{CTA_BUTTON_2}}</text>
  <text x="{{CENTER_X}}" y="980" class="subtext" text-anchor="middle">{{CTA_SUBTEXT}}</text>
  <text x="{{CENTER_X}}" y="{{BOTTOM_Y}}" class="handle" text-anchor="middle">{{BRAND_HANDLE}}</text>
</svg>
